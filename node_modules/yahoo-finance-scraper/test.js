const fs = require('fs').promises

const api = require('./index')

api('MSFT', '2009/01/01', '2019/01/01').then((result) => {
  const content = JSON.stringify(result)
  return fs.writeFile('output.json', content, 'utf8')
})
